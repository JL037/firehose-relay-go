module github.com/JL037/firehose-relay-go

go 1.24

// No external dependencies yet — the scaffold is stdlib-only so it builds
// and tests cleanly offline. The Postgres side (github.com/jackc/pgx) lands
// in internal/store during week 3; see docs/planning for the schedule.
