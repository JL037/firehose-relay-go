# results

Benchmark output, one JSON file per version, generated by replaying
`testdata/sample.jsonl` through each:

- `node.json` — the production Node pipeline's numbers
- `go.json` — this repo's numbers

The comparison table in the top-level `README.md` is generated from these
files, not eyeballed. Nothing here yet — populated once the benchmark harness
lands (weeks 4–5).
