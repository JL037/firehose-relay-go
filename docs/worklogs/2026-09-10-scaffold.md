# Project scaffold

- **Date:** 2026-09-10

## Done

- Cleaned up `README.md` (proper Markdown structure, real comparison table,
  task-list roadmap) and pushed to `main`.
- Scaffolded the repository to match the plan artifact's layout:
  - `cmd/relay/`, `cmd/capture/` — command entry points (skeleton `main`s).
  - `internal/firehose/`, `internal/store/`, `internal/stats/` — package docs,
    core types, and interface seams; implementations are TODO.
  - `testdata/`, `results/`, `deploy/` — with READMEs describing intent.
  - `docs/` — `planning/`, `worklogs/`, `lessons/`.
  - `Dockerfile`, `docker-compose.yml` (Postgres), `.github/workflows/ci.yml`.

## Notes / decisions

- Scaffold is stdlib-only and skeletal on purpose — structure and seams, not
  working logic. `go build/vet/test ./...` stays green.
- `pgx` deliberately not added to `go.mod` yet; it arrives with the week-3
  store implementation.

## Next

- Week 1–2 Go fundamentals (per the plan).
- First real code: `internal/firehose` — decode Jetstream events and implement
  `Filter.Match` over the 7 collection types.
