# deploy

Kubernetes manifests and monitoring config for running the relay in a local
`kind` cluster (week 6).

Planned contents:

- `deployment.yaml` — the relay Deployment (skeleton present)
- `service.yaml` — Service exposing the stats/metrics port (skeleton present)
- `grafana-dashboard.json` — one panel to start (events/sec over time); TODO
- Prometheus scrape config; TODO

These are scaffold stubs — see the manifests' inline `TODO`s.
